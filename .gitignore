# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/

# Raw UD clones: immutable + gitignored; only PROVENANCE.md committed (Spec §2.5, D28)
data/raw/*
!data/raw/PROVENANCE.md

# Derived corpus data (historical v2.1 layout); under HEXIS 3.1 no data is published in
# this tranche and the legal status of derivatives is undecided (plan §17.3).
data/interim/
data/processed/

# Local v3.1 corpus derivatives; publication is separate.
results/hexis31/

# Finder metadata (one copy entered and left the V0 deposit; see docs/HANDOFF.md)
.DS_Store
