# HORMATHOS — active specification (design HEXIS 3.1)

**V3-001, adopted on 16 September 2026; V3-002, realignment of the repository, 22 September 2026; V3-003, project name, 23 September 2026.** The complete scientific and operational authority is the [deposited plan](contracts/hexis-3.1/HEXIS_piano_definitivo_v3.1_2026-09-15.md) and the four JSON files identified by its `design_lock.json`. The text governs semantics, the JSON files fix values and lists. There are no alternative application parameters: every field of the YAML projection is compared with the deposit.

A descriptive study of within-sentence order in the annotations of the finite Greek corpus UD Perseus r2.18. Inventory of 17 documents, 11 primary in seven blocks and six `inventory_only`. Representations with ADV/PART merged; frozen CTW implemented and verified in V2; Q/G/four-CE profiles, two weightings and R1 as the contract prescribes. HORMATHOS is the project and its software; HEXIS 3.1 is the name of the deposited design it implements, and `hexis` survives only in the identifiers the contract fixes (V3-003).

**V0–V5 completed. The campaign (490 pairs/980 models) ran on 24 September 2026 in `results/hexis31/v3-seed0-v3006`, which holds seed 0 (V3), re-attested after its review under V3-005 and after the pre-V4 review under V3-006; its report was published and verified on 25 September, and the statement of results and limitations closed V5 on the same day ([results](RESULTS.md)). V3-007 published the finished results on 25 September.** Properties verified on synthetic and analytical fixtures keep their credit; the V2 attestation of `0dc69b5` remains evidence for that commit and has been re-attested after the realignment and after the rename ([handoff](HANDOFF.md)). Stop for review before any new run or publication; no real fit beyond V4. Fits start only in V3, after V0–V2. Corpus completion is distinct from scientific completion.

The active tree holds the 3.1 path only. Everything else — v2.1, G0/G1, `candidates/`, statistical utilities, the previous proposal, pre-audit results — is preserved byte for byte in `archive/`, at the path it had at `5f1ec06`, as history whose status is declared in [V3-002](02_DECISION_LOG.md) and without authority over new runs. No historical open item is declared resolved.

A scientific divergence stops the affected passage: it requires an explicit correction of the contract, never an update of the expected values to obtain a green test.
