> **Non-normative companion translation; the Italian original governs** (V3-003, B2).
> Original: `docs/contracts/hexis-3.1/HEXIS_allegati_v3.1_2026-09-15/LEGGIMI.md`, SHA-256 `ec62c4055c1d0829f5e4c739b02b989c97c26df53181ca94e7d1869cdb9d5598`.
> Translated on 2026-09-25. Values and identifiers follow the original, including file names. Any translator's note is marked *[Translator's note: …]* and changes nothing in the original. This companion is kept outside the attachment folder, whose file list the code checks strictly.

# HEXIS 3.1 — Attachments of the definitive plan

Date: 15 September 2026. Status: FINAL_PLAN_NOT_APPLIED.

The complete plan is the sibling file `HEXIS_piano_definitivo_v3.1_2026-09-15.md`. These JSON files update registry, expected corpus, alphabets, six cells and report contract. They are not an override of the v2.1 CLI nor results of the campaign. They contain no raw data or new real fits.

Keep the folder together with the plan, the `hexis-verifica` package and the historical ZIP identified in the contracts. The semantics is in the plan; the values must coincide. Deposit plan/contracts/design_lock in V0; the deposit is still null. The files of the repository are unchanged.

`SHA256SUMS.json` lists the files of the delivery and the plan through `../`; `design_lock.json` binds only plan and contracts, without recursive hashes. `verifica_documentale.json` describes the checks really performed; it is not a certificate of the future software gates.

The executable references remain in the historical ZIP (hashes and paths in the contract). Extract only the fixtures for the tests: some historical mains run real fits or use obsolete rules. No v3.1 test is attested before the porting.
