"""Artifact manifests: git commit, dirty flag, resolved-config sha256, input hashes,
package versions, timestamps, seed — one manifest per artifact (Spec §6.4; D26/D27).

Interface not specified in Spec §6.2; signatures will be proposed for approval
before implementation — no invented contracts.
"""
