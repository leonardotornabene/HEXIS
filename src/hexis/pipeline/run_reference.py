"""Stage run_reference (Spec §6.1): reference models (protocol (a)) + descriptive
readings — h_online, depth histograms, gain curves, context lexicon, root
distributions (Spec §4.2, §4.5; gate G4). Real-data fits: post-G2 only (D30)."""


def main() -> None:
    raise NotImplementedError


if __name__ == "__main__":
    main()
